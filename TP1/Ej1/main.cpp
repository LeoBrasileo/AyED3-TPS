#include <iostream>
#include <vector>

using namespace std;

int N, K;

//devuelve la matriz de n*n que sea cuadrado magico y orden lexicografico k
vector<vector<int>> cuadradoMagico(int n, int k){
    return vector<vector<int>> (n, vector<int>(n));
}

int main() {
    // Leo la entrada
    cin >> N >> K;

    //cout << N << K;
    return 0;
}